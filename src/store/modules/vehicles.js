

const state = {
    vehicle: []
};

const getters = {
};

const actions = {
};
//v-bind:to="{name: 'view-vehicle', params: {vehicle_id: vehicle.id}}
const mutations = {
};

export default { 
    namespaced: true,
    state,
    getters,
    actions,
    mutations
}